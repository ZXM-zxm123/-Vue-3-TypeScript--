# -Vue-3-TypeScript--
前端 Vue 3 + TypeScript 实现账单上传、分类展示、仪表盘图表（折线图、饼图）。Node.js 提供用户认证、账单存储（SQLite）、预算规则管理。Python 服务（Flask）提供 NLP 分类接口：接收交易描述，返回预测类别（训练一个简单朴素贝叶斯或调用 本地模型）。用户可训练自定义规则。实现预算设置（如每月餐饮 3000 元），超限提醒。支持多账户（现金、信用卡）和 csv 模板匹配。报告导出为 PDF。输出前端、Node.js、Python 代码。
